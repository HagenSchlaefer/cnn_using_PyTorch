# cnn_using_PyTorch
cnn using PyTorch to classify MNIST data

Code zum erstellen einer Virtuellen Umgebung

python -m venv .venv source .venv/bin/activate # Windows: .venv\Scripts\activate pip install -r requirements.txt

